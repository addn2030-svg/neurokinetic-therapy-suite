# === Secrets — NEVER commit ===
.env
.env.*
!.env.example
*.pem
*.key
credentials/
service-account*.json
*-service-account.json

# === Agent runtime state (belongs in Supabase now, not Git) ===
state.json
audit.jsonl

# === Python ===
__pycache__/
*.pyc
.venv/
venv/

# === Node ===
node_modules/

# === OS / Editor ===
.DS_Store
Thumbs.db
